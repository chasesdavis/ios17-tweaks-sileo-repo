version: 2
goal: device-test-tranche-1
tasks:
  - id: T001
    type: scout
    assignee: Scout
    status: active
    objective: "Prepare the device test matrix and install order from the built packages."
    inputs:
      - ../../audit.md
      - ../../../repo/debs
    constraints:
      - "Read-only task."
      - "Do not install or mutate packages from Scout."
    expected_output:
      - "A compact test matrix and risk order."
    receipt: null
  - id: T002
    type: worker
    assignee: Worker
    status: queued
    objective: "Run manual install/respring tests and record results."
    allowed_files:
      - docs/audit.md
      - docs/goals/device-test-tranche-1/notes
    verify:
      - "Each tweak has an explicit pass/fail/manual-blocked result."
    stop_if:
      - "Device is unavailable."
      - "A tweak causes repeated SpringBoard crashes."
    receipt: null
