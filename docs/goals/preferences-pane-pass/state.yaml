version: 2
goal: preferences-pane-pass
tasks:
  - id: T001
    type: judge
    assignee: Judge
    status: active
    objective: "Choose which tranche 1 tweaks deserve preferences based on device-test receipts."
    inputs:
      - ../device-test-tranche-1/state.yaml
      - ../../audit.md
    constraints:
      - "Read-only task."
    expected_output:
      - "Preference scope decision for each stable tweak."
    receipt: null
  - id: T002
    type: worker
    assignee: Worker
    status: queued
    objective: "Implement the approved minimal preference panes."
    allowed_files:
      - tweaks
      - docs/audit.md
    verify:
      - "./scripts/build-all.sh"
      - "./scripts/audit-tweaks.sh"
    stop_if:
      - "A preference requires unclear runtime behavior."
      - "Build fails twice."
    receipt: null
