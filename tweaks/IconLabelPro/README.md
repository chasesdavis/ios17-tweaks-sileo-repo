# IconLabelPro

IconLabelPro tightens SpringBoard icon label typography and hides labels for dock icons.

Build:

```bash
THEOS=/Users/chase/theos make clean package THEOS_PACKAGE_SCHEME=roothide
```
