# GhostDock

GhostDock fades the SpringBoard dock after a short idle delay and reveals it when the dock is touched or panned.

Build:

```bash
THEOS=/Users/chase/theos make clean package THEOS_PACKAGE_SCHEME=roothide
```
