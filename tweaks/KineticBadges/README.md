# KineticBadges

KineticBadges animates SpringBoard notification badge changes with a compact pop animation.

Build:

```bash
THEOS=/Users/chase/theos make clean package THEOS_PACKAGE_SCHEME=roothide
```
