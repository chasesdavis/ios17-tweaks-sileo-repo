# VolumeRibbon

VolumeRibbon styles volume HUD surfaces as compact translucent ribbons when matching SpringBoard classes are present.

Build:

```bash
THEOS=/Users/chase/theos make clean package THEOS_PACKAGE_SCHEME=roothide
```
