# VelvetAlerts

VelvetAlerts gives SpringBoard notification and banner views a softer radius, shadow, and entrance transition.

Build:

```bash
THEOS=/Users/chase/theos make clean package THEOS_PACKAGE_SCHEME=roothide
```
