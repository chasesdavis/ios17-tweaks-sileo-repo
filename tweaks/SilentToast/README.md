# SilentToast

SilentToast shows a small local toast when SpringBoard publishes a ringer or silent-state notification.

Build:

```bash
THEOS=/Users/chase/theos make clean package THEOS_PACKAGE_SCHEME=roothide
```
